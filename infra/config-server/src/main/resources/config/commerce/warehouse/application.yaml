spring:
  sql:
    init: always
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/sh_commerce_warehouse
    username: user_warehouse
    password: 12345

logging:
  level:
    ru.yandex.practicum: DEBUG
    org:
      hibernate.SQL: TRACE
      springframework: INFO